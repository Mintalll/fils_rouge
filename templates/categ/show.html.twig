{% extends 'base.html.twig' %}

{% block title %}Categ{% endblock %}

{% block body %}
    <h1>Categ</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categ.id }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ categ.categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categ_index') }}">back to list</a>

    <a href="{{ path('categ_edit', {'id': categ.id}) }}">edit</a>

    {{ include('categ/_delete_form.html.twig') }}
{% endblock %}
